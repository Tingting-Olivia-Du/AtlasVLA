# Atlas VLA 架构改进文档

本文档详细说明了针对训练不收敛问题实施的6项架构改进和超参数调整。

## 📋 改进概览

| 改进项 | 优先级 | 状态 | 文件位置 |
|--------|--------|------|----------|
| 改进1: 动作归一化 | 🔴 高 | ✅ 已完成 | `atlas/src/data/action_normalizer.py` |
| 改进2: Diffusion Action Head | 🟡 中 | ✅ 已完成 | `atlas/src/models/diffusion_action_head.py` |
| 改进3: Quaternion表示 | 🔴 高 | ✅ 已完成 | `atlas/src/models/action_head.py` |
| 改进4: 多帧时序训练 | 🟡 中 | ✅ 已完成 | `atlas/src/data/libero_dataset.py` |
| 改进5: Attention Pooling | 🟡 中 | ✅ 已完成 | `atlas/src/models/fusion.py` |
| 改进6: 辅助损失 | 🟢 低 | ✅ 已完成 | `atlas/src/training/loss.py` |
| 改进7: 超参数调整 | 🔴 高 | ✅ 已完成 | `atlas/configs/train_config.yaml` |

---

## 🔴 改进1: 动作归一化

### 问题分析

**问题**：
- 位置坐标（x, y, z）和旋转角度（roll, pitch, yaw）的尺度差异很大
- 不同任务的动作范围可能差异很大
- 未归一化的动作导致loss难以收敛

**解决方案**：
创建 `ActionNormalizer` 类，分别归一化位置和旋转：

```python
from atlas.src.data.action_normalizer import ActionNormalizer

# 使用示例
normalizer = ActionNormalizer(stats_path="action_stats.pt")
normalized_action = normalizer.normalize(action)  # 归一化
original_action = normalizer.denormalize(normalized_action)  # 反归一化
```

### 实现细节

- **位置归一化**：`(pos - pos_mean) / pos_std`
- **旋转归一化**：`(rot - rot_mean) / rot_std`
- **夹爪**：保持在 [0, 1] 范围，不归一化

### 配置方法

在 `train_config.yaml` 中：
```yaml
data:
  normalize_actions: true  # 启用动作归一化
  action_stats_path: null  # null=自动计算，或指定统计文件路径
```

### 预期效果

- ✅ 解决不同动作维度的尺度差异问题
- ✅ 提高训练稳定性
- ✅ 加速收敛

---

## 🔴 改进3: Quaternion旋转表示

### 问题分析

**问题**：
- 欧拉角（roll, pitch, yaw）存在**万向锁（Gimbal Lock）**问题
- 在特定角度下会出现奇异性，导致训练不稳定
- 旋转的连续性较差

**解决方案**：
使用四元数（Quaternion）表示旋转，避免奇异性问题。

### 实现细节

在 `ActionHead` 中添加 `use_quaternion` 选项：

```python
# 欧拉角模式（默认）
action_head = ActionHead(use_quaternion=False)  # 输出7维：[x,y,z,r,p,y,gripper]

# 四元数模式（推荐）
action_head = ActionHead(use_quaternion=True)  # 输出8维：[x,y,z,qw,qx,qy,qz,gripper]
```

**四元数归一化**：
```python
quat = F.normalize(quat_raw, p=2, dim=-1)  # 确保是单位四元数
```

### 配置方法

```yaml
model:
  use_quaternion: true  # 启用四元数表示（推荐）
  action_dim: 8  # 如果use_quaternion=true，需要设置为8
```

### 预期效果

- ✅ 避免万向锁问题
- ✅ 提高旋转预测的稳定性
- ✅ 更好的旋转连续性

---

## 🟡 改进2: Diffusion Action Head

### 问题分析

**问题**：
- 简单的回归head无法处理动作的多模态分布
- 对于同一场景可能有多个合理的动作，回归只能预测平均值

**解决方案**：
实现 Diffusion-based Action Head，可以：
1. 处理多模态动作分布
2. 生成更稳定的预测
3. 可以采样多个候选动作

### 架构设计

```
输入: fused_features [B, 1024]
  ↓
条件投影: [B, 256]
  ↓
时间步嵌入: [B, 256]
  ↓
U-Net噪声预测器
  ↓
输出: 动作 [B, 7]
```

### 使用方式

```python
from atlas.src.models.diffusion_action_head import DiffusionActionHead

# 训练时
diffusion_head = DiffusionActionHead(input_dim=1024, action_dim=7)
outputs = diffusion_head(fused_features, actions=gt_actions, timesteps=None)

# 推理时
outputs = diffusion_head(fused_features)  # 逐步去噪生成动作
```

### 配置方法

```yaml
model:
  use_diffusion_head: true  # 启用Diffusion Head（可选）
```

### 预期效果

- ✅ 处理多模态动作分布
- ✅ 更稳定的预测
- ✅ 可以采样多个候选动作

**注意**：Diffusion Head会增加训练时间和显存占用，建议在基础改进无效时再使用。

---

## 🟡 改进4: 多帧时序训练

### 问题分析

**问题**：
- 当前实现是单帧训练，没有利用时序信息
- VGGT Aggregator的Global Attention无法发挥时序建模作用

**解决方案**：
在Dataset中支持采样连续的多帧，利用时序信息。

### 实现细节

在 `LIBERODataset` 中添加参数：
- `num_temporal_frames`: 时序帧数（默认1，单帧）
- `temporal_stride`: 帧之间的步长（默认1）

```python
# 单帧模式（原始行为）
dataset = LIBERODataset(num_temporal_frames=1)

# 多帧时序模式
dataset = LIBERODataset(num_temporal_frames=4, temporal_stride=1)
```

### 数据格式变化

- **单帧**：`images: [S, 3, H, W]` （S=相机数）
- **多帧**：`images: [T*S, 3, H, W]` （T=时序帧数，S=相机数）

### 配置方法

```yaml
data:
  num_temporal_frames: 4  # 使用4帧时序（1=单帧）
  temporal_stride: 1  # 帧之间的步长
```

### 预期效果

- ✅ 利用时序信息
- ✅ 提高动作预测的准确性
- ✅ 更好地理解动作序列

**注意**：多帧训练会增加显存占用，建议根据GPU显存调整batch_size。

---

## 🟡 改进5: Attention Pooling

### 问题分析

**问题**：
- 当前Fusion模块使用简单的mean pooling聚合语言特征
- 丢失了细粒度的语言token信息
- 无法捕捉关键指令词

**解决方案**：
使用learnable query token + attention pooling替代mean pooling。

### 架构对比

**原始方法**：
```python
fused = fused.mean(dim=1)  # 简单的mean pooling
```

**改进方法**：
```python
query = self.lang_query.expand(B, -1, -1)  # Learnable query
fused_pooled, _ = self.lang_attn_pool(query, fused, fused)  # Attention pooling
```

### 配置方法

```yaml
model:
  use_attention_pooling: true  # 启用attention pooling（推荐）
```

### 预期效果

- ✅ 保留细粒度的语言信息
- ✅ 提高对关键指令词的敏感度
- ✅ 更好的多模态融合

---

## 🟢 改进6: 辅助损失

### 问题分析

**问题**：
- 只有主损失（pose + gripper），可能无法充分约束模型
- 缺少对中间特征的约束

**解决方案**：
添加辅助损失：
1. **几何一致性损失**：鼓励不同帧的几何特征一致性
2. **特征正则化损失**：防止特征过度拟合

### 实现细节

```python
# 几何一致性损失
geom_mean = geom_feat.mean(dim=1, keepdim=True)
geom_consistency = F.mse_loss(geom_feat, geom_mean.expand_as(geom_feat))

# 特征正则化损失
feature_reg = fused_feat.norm(dim=-1).mean()
```

### 配置方法

```yaml
training:
  loss:
    use_auxiliary_loss: false  # 是否启用（默认关闭）
    geom_consistency_weight: 0.1  # 几何一致性损失权重
    feature_reg_weight: 0.01  # 特征正则化损失权重
```

### 预期效果

- ✅ 提高训练稳定性
- ✅ 防止过拟合
- ✅ 更好的特征学习

**注意**：辅助损失会增加训练复杂度，建议在基础改进无效时再启用。

---

## 🔴 改进7: 超参数调整

### 调整内容

基于训练不收敛的问题，调整了以下超参数：

| 参数 | 原始值 | 新值 | 原因 |
|------|--------|------|------|
| `learning_rate` | 0.0001 | **0.00005** | 降低学习率提高稳定性 |
| `weight_decay` | 0.01 | **0.001** | 降低正则化强度 |
| `warmup_steps` | 1000 | **2000** | 增加warmup让学习率更平滑 |

### 配置位置

所有超参数在 `atlas/configs/train_config.yaml` 中配置。

### 预期效果

- ✅ 更稳定的训练过程
- ✅ 减少loss波动
- ✅ 更好的收敛性

---

## 🚀 使用建议

### 优先级推荐

1. **立即实施**（高优先级）：
   - ✅ 改进1: 动作归一化
   - ✅ 改进3: Quaternion表示
   - ✅ 改进7: 超参数调整

2. **短期改进**（中优先级）：
   - ✅ 改进4: 多帧时序训练
   - ✅ 改进5: Attention Pooling

3. **长期优化**（低优先级）：
   - ✅ 改进2: Diffusion Head（需要较大改动）
   - ✅ 改进6: 辅助损失（可选）

### 快速开始

1. **启用基础改进**：
```yaml
# train_config.yaml
model:
  use_quaternion: true
  use_attention_pooling: true

data:
  normalize_actions: true

training:
  learning_rate: 0.00005
  weight_decay: 0.001
  warmup_steps: 2000
```

2. **计算动作统计信息**（如果启用归一化）：
```python
from atlas.src.data.action_normalizer import ActionNormalizer
import numpy as np

# 收集动作数据
actions = []  # 从dataset收集所有动作
actions_array = np.array(actions)

# 计算统计信息
normalizer = ActionNormalizer()
stats = normalizer.compute_stats(actions_array)
normalizer.save_stats("action_stats.pt")
```

3. **训练模型**：
```bash
python atlas/train.py --config atlas/configs/train_config.yaml
```

---

## 📊 预期改进效果

### Loss收敛

- **原始**：loss在0.56左右波动，不收敛
- **改进后**：loss应该稳定下降，最终收敛到更低的值

### 训练稳定性

- **原始**：训练不稳定，loss波动大
- **改进后**：训练更稳定，loss平滑下降

### 动作预测准确性

- **原始**：动作预测误差较大
- **改进后**：动作预测更准确，特别是旋转预测

---

## 🔧 故障排除

### 问题1: 动作归一化后loss变小

**原因**：这是正常的，因为动作被归一化到更小的范围。

**解决**：关注相对改进，而不是绝对loss值。

### 问题2: 四元数模式下action_dim不匹配

**原因**：四元数模式需要8维（3 pos + 4 quat + 1 gripper），而不是7维。

**解决**：
```yaml
model:
  use_quaternion: true
  action_dim: 8  # 必须设置为8
```

### 问题3: 多帧训练显存不足

**原因**：多帧训练会增加显存占用。

**解决**：
- 减少 `num_temporal_frames`
- 减少 `batch_size`
- 使用 `gradient_accumulation_steps`

---

## 📝 代码变更总结

### 新增文件

1. `atlas/src/data/action_normalizer.py` - 动作归一化模块
2. `atlas/src/models/diffusion_action_head.py` - Diffusion Action Head

### 修改文件

1. `atlas/src/data/libero_dataset.py` - 添加多帧时序和动作归一化支持
2. `atlas/src/models/action_head.py` - 添加四元数表示选项
3. `atlas/src/models/fusion.py` - 添加attention pooling
4. `atlas/src/training/loss.py` - 添加辅助损失
5. `atlas/configs/train_config.yaml` - 更新配置参数

---

## 📚 参考文献

1. **Quaternion vs Euler Angles**: 
   - 四元数避免了万向锁问题，是旋转表示的更好选择

2. **Diffusion Models for Robotics**:
   - Diffusion models可以处理多模态分布，适合动作预测任务

3. **Attention Mechanisms**:
   - Attention pooling比mean pooling能保留更多信息

---

## ✅ 检查清单

在开始训练前，请确认：

- [ ] 动作归一化已启用（如果使用）
- [ ] 动作统计信息已计算（如果启用归一化）
- [ ] 四元数模式配置正确（action_dim匹配）
- [ ] 超参数已更新（learning_rate, weight_decay, warmup_steps）
- [ ] 多帧时序参数合理（根据GPU显存调整）
- [ ] 配置文件已保存

---

**最后更新**: 2026-02-07
**作者**: Atlas VLA Team
